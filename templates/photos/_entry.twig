{% set photos = craft.assets().volume('s3Photos').kind('image').all() %}
<ul>
    {% for photo in photos %}
        <li>
            <img src="{{ photo.getUrl('thumb') }}">
        </li>
    {% endfor %}
</ul>
